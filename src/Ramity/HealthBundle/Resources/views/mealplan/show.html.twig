{% extends 'base.html.twig' %}

{% block body %}
    <h1>Mealplan</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mealPlan.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mealPlan.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('mealplan_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('mealplan_edit', { 'id': mealPlan.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
